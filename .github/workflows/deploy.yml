name: Deploy to Netlify

on:
  push:
    tags: ["v*"] # Trigger on version tags like v1.0.0, v1.2.3, etc.

permissions:
  contents: write
  pages: write
  id-token: write

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout source code
        uses: actions/checkout@v4

      - name: Setup build environment
        uses: ./.github/actions/setup-system-deps
        with:
          rust-targets: "wasm32-unknown-unknown"
          cache-key-suffix: "deploy"

      - name: Install wasm-pack
        uses: jetli/wasm-pack-action@v0.4.0
        with:
          version: "latest"

      - name: Check wasm-bindgen versions
        run: |
          echo "🔍 Checking wasm-bindgen version consistency..."

          # Get version from Cargo.toml (handle both "0.2" and "0.2.95" formats)
          CARGO_VERSION=$(grep 'wasm-bindgen = ' Cargo.toml | head -1 | sed 's/.*"\([^"]*\)".*/\1/')
          echo "📦 Cargo.toml wasm-bindgen version: $CARGO_VERSION"

          # Check wasm-pack version
          WASM_PACK_VERSION=$(wasm-pack --version | cut -d' ' -f2)
          echo "🛠️ wasm-pack version: $WASM_PACK_VERSION"

          # Skip wasm-bindgen CLI installation for shortened versions
          if [[ "$CARGO_VERSION" =~ ^[0-9]+\.[0-9]+$ ]]; then
            echo "⚠️ Shortened version format detected ($CARGO_VERSION). Using wasm-pack's bundled wasm-bindgen."
            echo "✅ Will use --mode no-install to ensure wasm-pack uses its own version"
          else
            # Ensure wasm-bindgen CLI version matches for full versions
            if ! command -v wasm-bindgen &> /dev/null; then
              echo "📥 Installing wasm-bindgen CLI to match Cargo version..."
              cargo install wasm-bindgen-cli --version "$CARGO_VERSION"
            else
              CLI_VERSION=$(wasm-bindgen --version | cut -d' ' -f2)
              if [ "$CLI_VERSION" != "$CARGO_VERSION" ]; then
                echo "⚠️ Version mismatch detected. Reinstalling wasm-bindgen CLI..."
                cargo install wasm-bindgen-cli --version "$CARGO_VERSION" --force
              else
                echo "✅ wasm-bindgen versions match: $CARGO_VERSION"
              fi
            fi
          fi

      - name: Build WASM package
        run: |
          echo "🚀 Building Space Looter for tag: ${{ github.ref_name }}"
          chmod +x build-web.sh
          ./build-web.sh

      - name: Verify build output
        run: |
          echo "🔍 Verifying build output..."
          echo "Contents of dist directory:"
          ls -la dist/
          echo ""

          # Check for critical files
          if [ ! -f "dist/index.html" ]; then
            echo "❌ ERROR: index.html missing from build"
            exit 1
          fi

          if [ ! -f "dist/space_looter.js" ]; then
            echo "❌ ERROR: space_looter.js missing from build"
            exit 1
          fi

          if [ ! -f "dist/space_looter_bg.wasm" ]; then
            echo "❌ ERROR: space_looter_bg.wasm missing from build"
            echo "Build may have failed. Checking pkg directory:"
            ls -la pkg/ || echo "pkg directory not found"
            exit 1
          fi

          echo "✅ All critical files present in build"

      - name: Add version info and show statistics
        run: |
          echo "📁 Adding version info to HTML..."
          sed -i "s|<title>Space Looter</title>|<title>Space Looter ${{ github.ref_name }}</title>|g" dist/index.html

          echo "📊 Build statistics:"
          du -h dist/*

          echo "🔍 Final dist directory check before staging:"
          ls -la dist/

          # Add aggressive cache busting to force new WASM deployment
          echo "🔄 Adding cache busting with build info..."
          TIMESTAMP=$(date +%s)
          BUILD_HASH=$(echo "${{ github.sha }}" | cut -c1-8)
          BUILD_NUMBER="${{ github.run_number }}"
          CACHE_BUST="${BUILD_HASH}-${BUILD_NUMBER}-${TIMESTAMP}"

          cd dist
          echo "🎯 Cache bust identifier: ${CACHE_BUST}"

          # Rename files with aggressive cache busting
          if [ -f space_looter_bg.wasm ]; then
            mv space_looter_bg.wasm space_looter_bg.${CACHE_BUST}.wasm
            echo "✅ WASM file renamed to space_looter_bg.${CACHE_BUST}.wasm"
          fi

          if [ -f space_looter.js ]; then
            mv space_looter.js space_looter.${CACHE_BUST}.js
            echo "✅ JS file renamed to space_looter.${CACHE_BUST}.js"
          fi

          # Update HTML file references
          sed -i "s/space_looter\.js/space_looter.${CACHE_BUST}.js/g" index.html
          sed -i "s/space_looter_bg\.wasm/space_looter_bg.${CACHE_BUST}.wasm/g" index.html
          echo "✅ HTML references updated with cache busting"

          # Add build info to HTML for verification
          sed -i "s|</head>|  <meta name=\"build-hash\" content=\"${BUILD_HASH}\">\n  <meta name=\"build-number\" content=\"${BUILD_NUMBER}\">\n  <meta name=\"build-timestamp\" content=\"${TIMESTAMP}\">\n</head>|" index.html
          echo "✅ Build metadata added to HTML"

          cd ..

          echo "🔍 Files after aggressive cache busting:"
          ls -la dist/

      - name: Prepare for deployment
        run: |
          echo "🧹 Preparing for deployment..."

          # Verify source files exist before deployment
          echo "🔍 Verifying source files..."
          echo "Contents of dist directory:"
          ls -la dist/

          echo "🔍 Critical files check:"
          echo "  index.html: $([ -f dist/index.html ] && echo "✅ EXISTS" || echo "❌ MISSING")"
          echo "  JS files: $(ls dist/space_looter*.js 2>/dev/null | wc -l) found"
          echo "  WASM files: $(ls dist/space_looter_bg*.wasm 2>/dev/null | wc -l) found"
          echo "  netlify.toml: $([ -f netlify.toml ] && echo "✅ EXISTS" || echo "❌ MISSING")"

          if [ ! -f "dist/index.html" ] || [ $(ls dist/space_looter*.js 2>/dev/null | wc -l) -eq 0 ] || [ $(ls dist/space_looter_bg*.wasm 2>/dev/null | wc -l) -eq 0 ]; then
            echo "❌ ERROR: Critical files missing from dist directory"
            exit 1
          fi

          echo "✅ All source files verified"

          # Create a temporary directory to store files before branch switch
          echo "📦 Creating temporary storage for deployment files..."
          mkdir -p /tmp/deploy-files

          # Copy all files we need to the temporary directory
          echo "📁 Storing build files..."
          cp -r dist/* /tmp/deploy-files/
          echo "✅ Build files stored"

          echo "📁 Storing config files..."
          cp netlify.toml /tmp/deploy-files/ 2>/dev/null && echo "✅ netlify.toml stored" || echo "⚠️ netlify.toml not found"
          cp netlify-build.sh /tmp/deploy-files/ 2>/dev/null && echo "✅ netlify-build.sh stored" || echo "⚠️ netlify-build.sh not found"
          cp README.md /tmp/deploy-files/ 2>/dev/null && echo "✅ README.md stored" || echo "⚠️ README.md not found"
          cp .gitignore /tmp/deploy-files/ 2>/dev/null && echo "✅ .gitignore stored" || echo "⚠️ .gitignore not found"

          echo "📁 Storing .github directory to prevent CI breakage..."
          cp -r .github /tmp/deploy-files/ 2>/dev/null && echo "✅ .github directory stored" || echo "⚠️ .github directory not found"

          echo "📦 Files stored in temporary directory:"
          ls -la /tmp/deploy-files/

      - name: Deploy to deploy branch
        run: |
          # Check if deploy branch exists on remote
          if git ls-remote --exit-code --heads origin deploy >/dev/null 2>&1; then
            echo "📥 Deploy branch exists, fetching it..."
          else
            echo "🆕 Deploy branch will be created as orphan..."
          fi

          # Create separate directory for deploy branch to avoid interfering with workflow
          echo "📁 Creating separate deploy directory..."
          mkdir -p /tmp/deploy-branch
          cd /tmp/deploy-branch

          # Initialize git in the separate directory
          git init
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"

          # Add remote
          git remote add origin https://github.com/${{ github.repository }}.git

          echo "🔍 Verifying temporary storage contents:"
          ls -la /tmp/deploy-files/

          # Specific WASM file verification before copy (cache-busted files)
          WASM_FILE=$(ls /tmp/deploy-files/space_looter_bg*.wasm 2>/dev/null | head -1)
          if [ -n "$WASM_FILE" ]; then
            echo "🔍 WASM file in temporary storage: $(basename "$WASM_FILE")"
            echo "  Size: $(du -h "$WASM_FILE" | cut -f1)"
            echo "  Size (bytes): $(wc -c < "$WASM_FILE")"
            echo "  Modified: $(stat -c %y "$WASM_FILE")"
            echo "  MD5: $(md5sum "$WASM_FILE" | cut -d' ' -f1)"
          else
            echo "❌ WASM file missing from temporary storage!"
            exit 1
          fi

          # Copy all files from temporary storage to deploy directory
          echo "📦 Copying files to deploy directory..."
          cp -r /tmp/deploy-files/* . && echo "✅ All files copied" || { echo "❌ Failed to copy files"; exit 1; }

          # Make netlify build script executable if it exists
          chmod +x netlify-build.sh 2>/dev/null || echo "⚠️ netlify-build.sh not found or already executable"

          # Create deployment info
          echo "📝 Creating deployment documentation..."
          echo "# Space Looter Deployment" > DEPLOYMENT.md
          echo "" >> DEPLOYMENT.md
          echo "**Version:** ${{ github.ref_name }}" >> DEPLOYMENT.md
          echo "**Built at:** $(date -u)" >> DEPLOYMENT.md
          echo "**Commit:** ${{ github.sha }}" >> DEPLOYMENT.md
          echo "**Build:** GitHub Actions #${{ github.run_number }}" >> DEPLOYMENT.md
          echo "" >> DEPLOYMENT.md
          echo "## About This Branch" >> DEPLOYMENT.md
          echo "" >> DEPLOYMENT.md
          echo "This is the \`deploy\` branch containing pre-built files for Netlify deployment." >> DEPLOYMENT.md
          echo "Files are automatically built and deployed by GitHub Actions." >> DEPLOYMENT.md
          echo "" >> DEPLOYMENT.md
          echo "### Key Files:" >> DEPLOYMENT.md
          echo "- \`index.html\` - Game entry point" >> DEPLOYMENT.md
          echo "- \`space_looter.js\` - Generated JavaScript" >> DEPLOYMENT.md
          echo "- \`space_looter_bg.wasm\` - Game WebAssembly binary" >> DEPLOYMENT.md
          echo "- \`netlify.toml\` - Netlify configuration" >> DEPLOYMENT.md
          echo "- \`assets/\` - Game assets (audio, fonts, icons)" >> DEPLOYMENT.md
          echo "" >> DEPLOYMENT.md
          echo "## Files" >> DEPLOYMENT.md
          echo "\`\`\`" >> DEPLOYMENT.md
          ls -la >> DEPLOYMENT.md
          echo "\`\`\`" >> DEPLOYMENT.md

          # Debug: Show what files are actually in the deploy branch
          echo "🔍 Files in deploy branch after copy:"
          ls -la
          echo ""
          echo "🔍 Checking for critical files:"
          echo "  index.html: $([ -f index.html ] && echo "✅ EXISTS" || echo "❌ MISSING")"
          echo "  JS files: $(ls space_looter*.js 2>/dev/null | wc -l) found"
          echo "  WASM files: $(ls space_looter_bg*.wasm 2>/dev/null | wc -l) found"
          echo "  netlify.toml: $([ -f netlify.toml ] && echo "✅ EXISTS" || echo "❌ MISSING")"

          # Detailed WASM file verification (cache-busted)
          DEPLOYED_WASM=$(ls space_looter_bg*.wasm 2>/dev/null | head -1)
          if [ -n "$DEPLOYED_WASM" ]; then
            echo "🔍 WASM file details: $(basename "$DEPLOYED_WASM")"
            echo "  Size: $(du -h "$DEPLOYED_WASM" | cut -f1)"
            echo "  Size (bytes): $(wc -c < "$DEPLOYED_WASM")"
            echo "  Modified: $(stat -c %y "$DEPLOYED_WASM")"
            echo "  MD5: $(md5sum "$DEPLOYED_WASM" | cut -d' ' -f1)"
          else
            echo "❌ WASM file missing!"
          fi

          # Compare with temporary storage
          TEMP_WASM=$(ls /tmp/deploy-files/space_looter_bg*.wasm 2>/dev/null | head -1)
          if [ -n "$TEMP_WASM" ] && [ -n "$DEPLOYED_WASM" ]; then
            echo "🔍 Comparing WASM files:"
            echo "  Temp file: $(basename "$TEMP_WASM")"
            echo "  Deployed file: $(basename "$DEPLOYED_WASM")"
            echo "  Temp file MD5: $(md5sum "$TEMP_WASM" | cut -d' ' -f1)"
            echo "  Deployed file MD5: $(md5sum "$DEPLOYED_WASM" | cut -d' ' -f1)"
            if cmp -s "$DEPLOYED_WASM" "$TEMP_WASM"; then
              echo "  ✅ WASM files match"
            else
              echo "  ❌ WASM files differ!"
            fi
          fi

          # Verify all critical files are present
          if [ ! -f index.html ] || [ $(ls space_looter*.js 2>/dev/null | wc -l) -eq 0 ] || [ $(ls space_looter_bg*.wasm 2>/dev/null | wc -l) -eq 0 ]; then
            echo "❌ ERROR: Critical files missing after copy!"
            echo "Current directory contents:"
            ls -la
            exit 1
          fi

          # Show final deployed files with cache-busted names
          echo "🎯 Final deployed files:"
          echo "  HTML: $(ls index.html 2>/dev/null || echo "MISSING")"
          echo "  JS: $(ls space_looter*.js 2>/dev/null || echo "MISSING")"
          echo "  WASM: $(ls space_looter_bg*.wasm 2>/dev/null || echo "MISSING")"
          echo "  Assets: $([ -d assets ] && echo "✅ Directory exists" || echo "❌ Missing")"

          # Show build metadata for verification
          echo "🏷️ Build metadata:"
          echo "  Git hash: ${BUILD_HASH:-$(echo "${{ github.sha }}" | cut -c1-8)}"
          echo "  Build number: ${BUILD_NUMBER:-${{ github.run_number }}}"
          echo "  Cache bust ID: $(ls space_looter_bg*.wasm 2>/dev/null | sed 's/space_looter_bg\.\(.*\)\.wasm/\1/' | head -1)"

          # Add all files
          git add .

          # Check if there are any changes to commit
          if git diff --cached --quiet; then
            echo "No changes to deploy"
          else
            # Show what's being committed
            echo "🔍 Files being committed:"
            git diff --cached --name-only

            # Commit the changes
            git commit -m "Deploy ${{ github.ref_name }}

            - Built from commit: ${{ github.sha }}
            - Build #${{ github.run_number }}
            - Generated at: $(date -u)"

            # Push to deploy branch
            git push origin deploy
            echo "✅ Deployed ${{ github.ref_name }} to deploy branch"

            # Final verification
            echo "🔍 Final deploy branch status:"
            git ls-tree --name-only HEAD

            # Check if deploy branch exists and handle accordingly
            if git ls-remote --exit-code --heads origin deploy >/dev/null 2>&1; then
              echo "📥 Fetching existing deploy branch..."
              git fetch origin deploy
              git checkout -B deploy origin/deploy
            else
              echo "🆕 Creating new orphan deploy branch..."
              git checkout --orphan deploy
            fi

            # Add all deployment files
            git add .urge and rebuild
            echo "🔄 Triggering Netlify cache purge..."

            # Create a cache-busting commit to force Netlify to rebuild
            echo "cache-bust: $(date +%s)" > .netlify-cache-bust
            git add .netlify-cache-bust
            git commit -m "Force Netlify cache purge - Build #${{ github.run_number }}"
            git push origin deploy

            echo "✅ Cache purge commit pushed to force Netlify rebuild"
            echo "🌐 Netlify will detect the new commit and redeploy automatically"
            echo "⏳ Wait 2-3 minutes for Netlify to rebuild and purge CDN cache"
          fi

          # Final verification from deploy directory
          echo "🔍 Final verification from deploy directory:"
          pwd
          ls -la

          # Return to original workspace to avoid workflow cleanup issues
          cd "${GITHUB_WORKSPACE}"
          echo "🏠 Returned to original workspace for cleanup"
        env:
          GITHUB_TOKEN: ${{ github.token }}

      - name: Create deployment summary
        run: |
          echo "## 🚀 Deployment Complete" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "**Tag:** ${{ github.ref_name }}" >> $GITHUB_STEP_SUMMARY
          echo "**Branch:** deploy" >> $GITHUB_STEP_SUMMARY
          echo "**Status:** ✅ Success" >> $GITHUB_STEP_SUMMARY
          echo "**Build:** #${{ github.run_number }}" >> $GITHUB_STEP_SUMMARY
          echo "**Commit:** $(echo "${{ github.sha }}" | cut -c1-8)" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### Files deployed with cache busting:" >> $GITHUB_STEP_SUMMARY
          echo "- index.html (with build metadata)" >> $GITHUB_STEP_SUMMARY
          echo "- space_looter.*.js (cache-busted)" >> $GITHUB_STEP_SUMMARY
          echo "- space_looter_bg.*.wasm (cache-busted)" >> $GITHUB_STEP_SUMMARY
          echo "- .netlify-cache-bust (forces rebuild)" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "🌐 **Netlify cache purge triggered - rebuilding now**" >> $GITHUB_STEP_SUMMARY
          echo "⏳ **Wait 2-3 minutes for CDN cache to clear**" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### Verification:" >> $GITHUB_STEP_SUMMARY
          echo "1. Check HTML source for build metadata" >> $GITHUB_STEP_SUMMARY
          echo "2. Check dev tools for cache-busted filenames" >> $GITHUB_STEP_SUMMARY
          echo "3. WASM file should be ~30MB" >> $GITHUB_STEP_SUMMARY
